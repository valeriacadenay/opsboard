/**
 * @deprecated Use LoggerService from core/logging/logger.service.ts instead
 * Este archivo se mantiene por compatibilidad, será removido en futuras versiones
 */

export { LoggerService } from '../logging/logger.service';
